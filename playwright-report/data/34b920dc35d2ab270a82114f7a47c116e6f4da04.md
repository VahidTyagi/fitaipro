# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: fitaipro.spec.ts >> FitAI Pro — Pre-Deploy Suite >> T16 — Authenticated: AI Coach chat loads
- Location: tests\e2e\fitaipro.spec.ts:209:7

# Error details

```
Error: locator.click: Target page, context or browser has been closed
Call log:
  - waiting for locator('button[type=\'submit\']').first()
    - locator resolved to <button type="submit" aria-hidden="true"></button>
  - attempting click action
    2 × waiting for element to be visible, enabled and stable
      - element is not visible
    - retrying click action
    - waiting 20ms
    2 × waiting for element to be visible, enabled and stable
      - element is not visible
    - retrying click action
      - waiting 100ms
    12 × waiting for element to be visible, enabled and stable
       - element is not visible
     - retrying click action
       - waiting 500ms

```

# Test source

```ts
  1   | import { test, expect, Page } from "@playwright/test";
  2   | 
  3   | const BASE =
  4   |   process.env.PLAYWRIGHT_TEST_BASE_URL ||
  5   |   "https://fitaipro-five.vercel.app";
  6   | 
  7   | // Test account — uses Clerk test magic OTP (424242), no real email needed
  8   | const TEST_EMAIL = "vahidtyagi007+clerk_test@gmail.com";
  9   | const TEST_PASSWORD = "Mlpqaz@23";
  10  | 
  11  | // ── Auth helper ──────────────────────────────────────────────────────────────
  12  | async function signInTestUser(page: Page) {
  13  |   await page.goto(`${BASE}/sign-in`, { waitUntil: "domcontentloaded" });
  14  |   await page.waitForTimeout(2000); // let Clerk load
  15  | 
  16  |   // Fill email
  17  |   const emailInput = page
  18  |     .locator("input[name='identifier'], input[type='email']")
  19  |     .first();
  20  |   await emailInput.waitFor({ timeout: 15_000 });
  21  |   await emailInput.fill(TEST_EMAIL);
> 22  |   await page.locator("button[type='submit']").first().click();
      |                                                       ^ Error: locator.click: Target page, context or browser has been closed
  23  |   await page.waitForTimeout(1500);
  24  | 
  25  |   // Fill password
  26  |   const pwInput = page
  27  |     .locator("input[name='password'], input[type='password']")
  28  |     .first();
  29  |   await pwInput.waitFor({ timeout: 10_000 });
  30  |   await pwInput.fill(TEST_PASSWORD);
  31  |   await page.locator("button[type='submit']").first().click();
  32  |   await page.waitForTimeout(2000);
  33  | 
  34  |   // Handle OTP if it appears (Clerk test accounts use 424242)
  35  |   const otpInput = page.locator("input[name='code'], input[aria-label*='digit']").first();
  36  |   const otpVisible = await otpInput.isVisible().catch(() => false);
  37  |   if (otpVisible) {
  38  |     await otpInput.fill("424242");
  39  |     await page.locator("button[type='submit']").first().click();
  40  |     await page.waitForTimeout(2000);
  41  |   }
  42  | 
  43  |   // Wait for dashboard
  44  |   await page.waitForURL(/dashboard/, { timeout: 20_000 });
  45  | }
  46  | 
  47  | // ════════════════════════════════════════════════════════════════════════════
  48  | test.describe("FitAI Pro — Pre-Deploy Suite", () => {
  49  | 
  50  |   // ── T01 ──────────────────────────────────────────────────────────────────
  51  |   test("T01 — Landing page loads with FitAI title", async ({ page }) => {
  52  |     await page.goto(BASE, { waitUntil: "domcontentloaded" });
  53  |     const title = await page.title();
  54  |     expect(title).toMatch(/FitAI/i);
  55  |   });
  56  | 
  57  |   // ── T02 ──────────────────────────────────────────────────────────────────
  58  |   test("T02 — Landing page has Sign Up CTA", async ({ page }) => {
  59  |     await page.goto(BASE, { waitUntil: "domcontentloaded" });
  60  |     const btn = page.locator("a[href='/sign-up']").first();
  61  |     await expect(btn).toBeVisible({ timeout: 10_000 });
  62  |   });
  63  | 
  64  |   // ── T03 ──────────────────────────────────────────────────────────────────
  65  |   test("T03 — Sign-in page returns 200", async ({ page }) => {
  66  |     const res = await page.goto(`${BASE}/sign-in`, {
  67  |       waitUntil: "domcontentloaded",
  68  |     });
  69  |     expect(res?.status()).toBe(200);
  70  |   });
  71  | 
  72  |   // ── T04 ──────────────────────────────────────────────────────────────────
  73  |   test("T04 — Sign-up page returns 200", async ({ page }) => {
  74  |     const res = await page.goto(`${BASE}/sign-up`, {
  75  |       waitUntil: "domcontentloaded",
  76  |     });
  77  |     expect(res?.status()).toBe(200);
  78  |   });
  79  | 
  80  |   // ── T05 ──────────────────────────────────────────────────────────────────
  81  |   test("T05 — Pricing page has plan names", async ({ page }) => {
  82  |     const res = await page.goto(`${BASE}/pricing`, {
  83  |       waitUntil: "domcontentloaded",
  84  |     });
  85  |     expect(res?.status()).toBe(200);
  86  |     const body = await page.textContent("body");
  87  |     expect(body).toMatch(/Pro/);
  88  |     expect(body).toMatch(/Free/);
  89  |   });
  90  | 
  91  |   // ── T06 ──────────────────────────────────────────────────────────────────
  92  |   test("T06 — Privacy page loads with content", async ({ page }) => {
  93  |     const res = await page.goto(`${BASE}/privacy`, {
  94  |       waitUntil: "domcontentloaded",
  95  |     });
  96  |     expect(res?.status()).toBe(200);
  97  |     const body = await page.textContent("body");
  98  |     expect(body).toMatch(/Privacy/i);
  99  |   });
  100 | 
  101 |   // ── T07 ──────────────────────────────────────────────────────────────────
  102 |   test("T07 — Terms page loads with content", async ({ page }) => {
  103 |     const res = await page.goto(`${BASE}/terms`, {
  104 |       waitUntil: "domcontentloaded",
  105 |     });
  106 |     expect(res?.status()).toBe(200);
  107 |     const body = await page.textContent("body");
  108 |     expect(body).toMatch(/Terms/i);
  109 |   });
  110 | 
  111 |   // ── T08 ──────────────────────────────────────────────────────────────────
  112 |   test("T08 — Dashboard redirects unauthenticated users", async ({ page }) => {
  113 |     await page.goto(`${BASE}/dashboard`, { waitUntil: "domcontentloaded" });
  114 |     await page.waitForTimeout(3000);
  115 |     const url = page.url();
  116 |     expect(url).toMatch(/sign-in|sign-up/);
  117 |   });
  118 | 
  119 |   // ── T09 ──────────────────────────────────────────────────────────────────
  120 |   test("T09 — API routes protected (401 without auth)", async ({ page }) => {
  121 |     const routes = [
  122 |       { url: `${BASE}/api/stats`,              method: "GET"  as const },
```